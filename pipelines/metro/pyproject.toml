[project]
name = "pulso-metro"
version = "0.1.0"
description = "Record CRTM Metro arrivals for Madrid into BigQuery, forever"
requires-python = ">=3.12"
dependencies = [
  "google-auth>=2.35",
  "google-cloud-bigquery>=3.25",
  "google-cloud-storage>=2.18",
  "httpx>=0.27",
]

[project.scripts]
pulso-metro = "pulso_metro.main:cli"

[dependency-groups]
dev = [
    "cryptography>=50.0.1",
    "mypy>=1.11",
    "pytest>=8",
    "ruff>=0.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pulso_metro"]

[tool.ruff]
line-length = 100

[tool.mypy]
python_version = "3.12"
files = ["src", "tests"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["google.cloud.*", "google.cloud"]
ignore_missing_imports = true
follow_imports = "skip"
