# Agent context + local planning docs (intentionally not committed)
AGENTS.md
CLAUDE.md
docs/
.claude/

# Secrets / credentials
.env
.env.*
!.env.example
*.pem
*-key.json
service-account*.json
application_default_credentials.json

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
*.egg-info/

# Node
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
npm-debug.log*
.pnpm-debug.log*

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
# .terraform.lock.hcl IS committed, for the same reason uv.lock is: it pins provider
# versions and hashes so every machine and CI resolve identically.

# Data — raw/large artifacts never belong in git
data/
data-samples/
*.pbf
*.gtfs.zip
*.parquet
*.pmtiles
*.gpkg
*.sqlite

# OS / editor
.DS_Store
.idea/
.vscode/
